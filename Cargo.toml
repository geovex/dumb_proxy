[package]
name = "proxy"
version = "0.1.0"
authors = ["Heorhi Valakhanovich <valahanovich@tut.by>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
tokio = {version = "0.2", features = ["full"] }
tokio-io-timeout = {version = "0.4"}
futures = "0.3"
config = {version = "0.10", features = ["toml", "yaml"]}
serde = "1.0"
serde_derive = "1.0"
nom = "5.1"

[profile.release]
lto = true